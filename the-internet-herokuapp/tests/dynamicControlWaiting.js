describe("waiting stretegies for dynamic control", () => {
    it("should wait for element to be enabled then click", async () => {
        //below is used to open browser in debug mode
        browser.debug();
        //inprogress
    })
})